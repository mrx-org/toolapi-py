[project]
name = "test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "toolapi",
]

[tool.uv.sources]
toolapi = { workspace = true, editable = true }
